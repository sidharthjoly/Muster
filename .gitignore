__pycache__/
*.py[cod]
.venv/
.env
*.egg-info/
.pytest_cache/
.DS_Store
fixtures/raw/
data/jobs.db
*.db
# WAL leaves these two beside the database; *.db does not cover them.
*.db-wal
*.db-shm
data/discovery/
data/meta_sitemap_probe.json
data/nab_sitemap_probe.json
data/logs/
site/
.neon
.env.local
node_modules/
.wrangler/
# `.claude/skills/` is vendored and pinned by skills-lock.json, so it stays.
# This one is per-machine — permission rules for whoever is sitting here — and
# a public repo is the wrong place for it. It was covered only by a global
# ignore on this laptop, which does nothing for a clone or for CI.
.claude/settings.local.json
